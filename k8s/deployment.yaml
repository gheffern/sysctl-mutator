apiVersion: apps/v1
kind: Deployment
metadata:
  name: sysctl-mutator
  namespace: sysctl-mutator
  labels:
    app: sysctl-mutator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sysctl-mutator
  template:
    metadata:
      labels:
        app: sysctl-mutator
    spec:
      serviceAccountName: sysctl-mutator
      containers:
        - name: webhook
          image: sysctl-mutator:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: DEFAULT_SYSCTLS
              value: '{"net.ipv4.ip_local_port_range": "1024 65000"}'
            # Set to "true" to disable namespace watching/reading permissions (Low-Privilege Mode)
            - name: DISABLE_NAMESPACE_REFLECTOR
              value: "true"
            - name: PORT
              value: "8443"
            - name: BIND_ADDRESS
              value: "0.0.0.0"
            - name: TLS_CERT
              value: "/etc/webhook/certs/tls.crt"
            - name: TLS_KEY
              value: "/etc/webhook/certs/tls.key"
            - name: HTTP2_KEEP_ALIVE_INTERVAL_SECS
              value: "0"
            - name: HTTP2_KEEP_ALIVE_TIMEOUT_SECS
              value: "20"
            - name: HTTP2_MAX_CONCURRENT_STREAMS
              value: "0"
            - name: DISABLE_METRICS
              value: "false"
            - name: METRICS_PORT
              value: "9090"
            - name: METRICS_BIND_ADDRESS
              value: "0.0.0.0"
          ports:
            - containerPort: 8443
              name: https
            - containerPort: 9090
              name: metrics
          volumeMounts:
            - name: certs
              mountPath: /etc/webhook/certs
              readOnly: true
          resources:
            limits:
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 64Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8443
              scheme: HTTPS
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8443
              scheme: HTTPS
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: certs
          secret:
            secretName: sysctl-mutator-certs
---
apiVersion: v1
kind: Service
metadata:
  name: sysctl-mutator
  namespace: sysctl-mutator
  labels:
    app: sysctl-mutator
spec:
  ports:
    - port: 443
      targetPort: 8443
      name: https
    - port: 9090
      targetPort: 9090
      name: metrics
  selector:
    app: sysctl-mutator
